import React, { useState } from "react";
import { Button, Image, View, Text } from "react-native";

const MultiImagePickerWithLocation = () => {
  
  return (
    <View style = {{flex: 1, padding: 100}}>
      <Text>MultiImagePickerWithLocation</Text>
    </View>
  );
};

export default MultiImagePickerWithLocation;
